import React from 'react';

const Course = ({course}) => {
    const {picture,name, _id, duration,rating,price}=course

    return (
        <div>
            
        </div>
    );
};

export default Course;